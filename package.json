{
  "name": "weather",
  "scripts": {
    "dev:build": "bun build src/background.ts src/popup.ts --outdir build --watch",
    "dev:typecheck": "bun x tsc --noEmit -w",
    "dev": "bun run dev:build & bun run dev:typecheck",
    "build": "bun build src/background.ts src/popup.ts --outdir build",
    "typecheck": "bun x tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
